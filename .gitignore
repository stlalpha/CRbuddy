# Build output
/coderabbit-buddy
*.test
*.out

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Personal Claude Code instructions — local only, not for the repo.
/CLAUDE.md

# Local dev-environment artifacts unrelated to this project (this checkout's
# directory is reused by other local tooling) — never meant to be tracked.
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
/.claude/
